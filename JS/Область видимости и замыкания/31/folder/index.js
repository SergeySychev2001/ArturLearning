function example() {
    let localVar = 'Local Variable';
    // Используем переменную только в этой функции
    console.log(localVar);
}
example();
// console.log(localVar); // ошибка: localVar не определена
