16. Как работать с JSONP для обхода ограничений CORS?
JSONP (JSON with Padding) позволяет загружать данные с другого домена, добавляя `<script>` тег, 
который вызывает функцию-обработчик, переданную в запросе.
