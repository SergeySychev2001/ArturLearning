31. Как работает контекст `this` в функциях?

Контекст `this` зависит от того, как функция вызывается. В обычных функциях `this` ссылается 
на глобальный объект (в браузере — `window`), а в методах объектов — на сам объект.