25. Что такое стек и очередь в контексте массивов?
- **Стек**: Структура данных LIFO (последний пришел - первый вышел). Используются методы `push` и `pop`.
- **Очередь**: Структура данных FIFO (первый пришел - первый вышел). Используются методы `push` и `shift`.
