Шаблонные строки — это строки, заключенные в обратные кавычки (`` ` ``), которые поддерживают интерполяцию и многострочность. 
Внутри шаблонной строки можно вставлять значения переменных и выполнять выражения, используя синтаксис `${...}`: