Как в ES6 реализована работа с прокси (Proxy)?
Proxy в ES6 позволяет создавать объект-обертку, который перехватывает операции, 
выполняемые над объектом. Создается с помощью конструктора `Proxy(target, handler)`