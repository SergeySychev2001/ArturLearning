13. Какие ограничения у ES6 модулей?
- ES6 модули используют строгий режим.
- Они не поддерживают динамические `require()` вызовы.
- Все импорты должны находиться на верхнем уровне модуля.
- Импортируемые значения являются неизменяемыми.